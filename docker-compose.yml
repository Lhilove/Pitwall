version: "3.8"

services:
  db:
    image: postgres:15
    container_name: pitwall-db
    environment:
      POSTGRES_USER: pitwalluser
      POSTGRES_PASSWORD: pitwallpass
      POSTGRES_DB: f1telemetry
    ports:
      - "5432:5432"